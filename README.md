# java-list-performance-benchmark
This repository benchmarks Java list implementations (ArrayList, LinkedList, etc.). Performance is measured for common operations like adding, removing, and iterating using JMH. The goal is to compare the efficiency of each list type for different use cases.
